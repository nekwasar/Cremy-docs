'use client';

import { ReactNode } from 'react';

export function HeroSection(): ReactNode {
  return (
    <div>
      <h1>Documents, done smoothly.</h1>
      <p>
        Create, convert, translate, and more - all in one place.
      </p>
      <div>
        <span>Animated Demo Area</span>
      </div>
    </div>
  );
}